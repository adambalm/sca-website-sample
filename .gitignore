# Dependencies
node_modules/

# Build outputs
dist/
.astro/
apps/sca-studio/sca-admin/   # Sanity Studio build output — regenerated by `sanity build`

# Environment files
.env
.env.local
.env.*.local
.env.preview

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Sanity
.sanity/

# Screenshots from testing (root-level only, not site images)
/*.png
screenshot*.png

# Playwright artifacts
playwright-report/
test-results/
test-screenshots/
tests/screenshots/
tests/**/*-snapshots/

# Extraction artifacts (one-time migration, not needed for build/runtime)
data/
recon/

# Agent-local settings
.claude/settings.local.json

# Windows artifacts
nul
.vercel

# Sensitive data / logs
.gemini/
users.csv
*.debug.log
firebase-debug.log

# Temp audit/fix scripts (session artifacts, not build dependencies)
_*.js
_*.mjs
pw-*.js
playwright-*.js
image-rollback*.json

# Local-only docs (drafts, comms, agent notes) — docs normalized 2026-04-21
docs/sessions/comms/
docs/sessions/notes/
dialogues/

# Platform state (runtime probe output, not source)
platform-state.yml

# GWS / Google Workspace automation scripts
# These are NOT website code. They live here temporarily for convenience but
# belong in a separate repo (sca-internal-tools or similar).
# Contains Google OAuth tokens, doc IDs, and admin credentials — never commit.
scripts/offer-letter-automation/

# Scratch / temporary directories (session artifacts, never commit)
tmp-*/
tmp/
_provision-output/
_visual-audit/
audit-tests/
site-comparison/
tmp-designer-review/
gdi/
planning/
templates/

# Scratch files at repo root
$null
body.json
tmp-*.sh
tmp-*.mjs
tmp-*.csv
tmp-*.json
tmp-*.md
tmp-*.png
tmp-*.txt

# Claude Code local workspace
.claude/
